// Q1. Create an interface Person with:
   // firstName (string)
   // lastName (string)
   // getFullName() → returns string
   // description (optional string)


// Q2. a) Create:
    // Animal interface with:
        // name (string)
        // age (number)

// b) Then create:
    // Dog interface that extends Animal
    // breed (string)

// Q3. interface User {
        //   id: number;
        //   name: string;
        //   email: string;
        //   password: string;
        // }
    // Now create a new interface UserWithoutPassword
